/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "cdn.shopify.com",
      "flagsapi.com",

      "www.shortformbooks.com",
    ],
  },
};

module.exports = nextConfig;
